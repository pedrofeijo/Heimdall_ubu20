Heimdall: The Lapisco's framework for region-based CNN
====

This installation suggestion is for Ubuntu 20.04

1. Run ` source Makefile install` to install python dependencies in a isolated environment;


